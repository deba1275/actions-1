name: Shell Script Test Workflow

on: push

jobs:
    shell_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4

        - name: List the files
          run: ls -ltra

        # - name: Change script permission 
        #   run: chmod +x ./cowsay.sh

        - name: Executing Shell Script
          run: |
            chmod +x ./cowsay.sh
            ./cowsay.sh

